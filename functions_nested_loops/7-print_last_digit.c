#include "main.h"
/**
 * print_last_digit - check the code
 * @n: in the integer
 * Return: change the return if
 */
int print_last_digit(int n)
{
	int a = n % 10;

	if (a < 0)
	{
		a *= -1;
	}
	_putchar((a + '0');
	return (a);
}
